version: '3.8'

services:
  lumina4k:
    build: .
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
      - NEXT_TELEMETRY_DISABLED=1
    volumes:
      - ./.env.local:/app/.env.local:ro
    restart: unless-stopped
